---
# Source: stamp/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: stamp
  labels:
    helm.sh/chart: stamp-0.1.0
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: stamp
    stamp.dev/topology: split
# The process talks to Postgres and to the IdP. It reads nothing from the
# Kubernetes API, so it is given no token to read it with.
automountServiceAccountToken: false
---
# Source: stamp/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: stamp-env
  labels:
    helm.sh/chart: stamp-0.1.0
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: stamp
    stamp.dev/topology: split
data:
  STAMP_OIDC_ISSUER: "https://idp.example"
  STAMP_OIDC_JWKS_URL: "https://idp.example/.well-known/jwks.json"
  STAMP_OIDC_AUDIENCE: "stamp"
  STAMP_OIDC_WORKLOAD_CLIENTS: "pep-1,svc-payments"
  STAMP_OIDC_ALLOW_INSECURE_TRANSPORT: "false"
  STAMP_APPROVER_ISSUER: "https://idp.example"

  STAMP_DB_MAX_CONNS: "24"
  STAMP_EGRESS_ALLOW: "https://risk.internal.example,https://directory.internal.example"
  STAMP_EGRESS_ALLOW_LOOPBACK: "false"
  STAMP_EGRESS_ALLOW_PRIVATE: "false"
  STAMP_FACT_ALLOW_FAIL_OPEN: "false"

  STAMP_AUDIT_FAIL_CLOSED: "true"

  STAMP_AUTHORING_MODE: "file"
  STAMP_GOVERNANCE_MIN_APPROVERS: "2"
  STAMP_GOVERNANCE_PROPOSER_MAY_APPROVE: "false"
  STAMP_CONSOLE_API_BASE_URL: "https://stamp-api.example"
  STAMP_CONSOLE_OIDC_CLIENT_ID: "stamp-console"
  STAMP_CONSOLE_OIDC_AUTHORIZATION_ENDPOINT: "https://idp.example/authorize"
  STAMP_CONSOLE_OIDC_TOKEN_ENDPOINT: "https://idp.example/token"
  STAMP_CONSOLE_OIDC_END_SESSION_ENDPOINT: "https://idp.example/logout"
  STAMP_CONSOLE_ALLOW_INSECURE_TRANSPORT: "false"
  STAMP_CALLBACK_BASE_URL: "https://stamp-callback.example"
  STAMP_MFA_ACR_VALUES: "urn:example:acr:mfa"
  STAMP_MFA_AUTHORIZATION_ENDPOINT: "https://idp.example/authorize"
  STAMP_MFA_CLIENT_ID: "stamp-stepup"
  STAMP_MFA_REDIRECT_URI: "https://stamp-callback.example/mfa/return"
  STAMP_MFA_CIBA_BACKCHANNEL_ENDPOINT: "https://idp.example/ciba"
  STAMP_MFA_CIBA_TOKEN_ENDPOINT: "https://idp.example/token"
  STAMP_MFA_CIBA_CLIENT_ID: "stamp-ciba"
  STAMP_KAFKA_BROKERS: "kafka-0.kafka:9092"
  STAMP_KAFKA_GROUP: "stamp-consumer"
  STAMP_EXTERNAL_TARGETS: "/etc/stamp/documents/external-targets.json"
  STAMP_FACT_SOURCES: "/etc/stamp/documents/fact-sources.json"
  STAMP_IDP_GROUP_SOURCES: "/etc/stamp/documents/idp-group-sources.json"
  STAMP_INGEST_CREDENTIALS: "/etc/stamp/documents/ingest-credentials.json"
  STAMP_KAFKA_TOPICS: "/etc/stamp/documents/kafka-topics.json"
  STAMP_STREAM_SOURCES: "/etc/stamp/documents/stream-sources.json"
---
# Source: stamp/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: stamp-check
  labels:
    helm.sh/chart: stamp-0.1.0
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: stamp
    stamp.dev/topology: split
    app.kubernetes.io/component: check
spec:
  type: ClusterIP
  # A Service publishes exactly the surfaces its tier binds. A check tier has no
  # console port to reach even by mistake, which is the point of splitting the
  # listeners rather than the paths.
  ports:
    - name: pep
      port: 8080
      targetPort: pep
      protocol: TCP
  selector:
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/component: check
---
# Source: stamp/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: stamp-decide
  labels:
    helm.sh/chart: stamp-0.1.0
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: stamp
    stamp.dev/topology: split
    app.kubernetes.io/component: decide
spec:
  type: ClusterIP
  # A Service publishes exactly the surfaces its tier binds. A check tier has no
  # console port to reach even by mistake, which is the point of splitting the
  # listeners rather than the paths.
  ports:
    - name: console
      port: 8081
      targetPort: console
      protocol: TCP
    - name: callback
      port: 8082
      targetPort: callback
      protocol: TCP
  selector:
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/component: decide
---
# Source: stamp/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: stamp-consumer
  labels:
    helm.sh/chart: stamp-0.1.0
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: stamp
    stamp.dev/topology: split
    app.kubernetes.io/component: consumer
spec:
  type: ClusterIP
  # A Service publishes exactly the surfaces its tier binds. A check tier has no
  # console port to reach even by mistake, which is the point of splitting the
  # listeners rather than the paths.
  ports:
    - name: callback
      port: 8082
      targetPort: callback
      protocol: TCP
  selector:
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/component: consumer
---
# Source: stamp/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: stamp-api
  labels:
    helm.sh/chart: stamp-0.1.0
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: stamp
    stamp.dev/topology: split
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  # A Service publishes exactly the surfaces its tier binds. A check tier has no
  # console port to reach even by mistake, which is the point of splitting the
  # listeners rather than the paths.
  ports:
    - name: console
      port: 8081
      targetPort: console
      protocol: TCP
  selector:
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/component: api
---
# Source: stamp/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: stamp-console
  labels:
    helm.sh/chart: stamp-0.1.0
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: stamp
    stamp.dev/topology: split
    app.kubernetes.io/component: console
spec:
  type: ClusterIP
  # A Service publishes exactly the surfaces its tier binds. A check tier has no
  # console port to reach even by mistake, which is the point of splitting the
  # listeners rather than the paths.
  ports:
    - name: console
      port: 8081
      targetPort: console
      protocol: TCP
  selector:
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/component: console
---
# Source: stamp/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stamp-check
  labels:
    helm.sh/chart: stamp-0.1.0
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: stamp
    stamp.dev/topology: split
    app.kubernetes.io/component: check
    stamp.dev/roles: "check"
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: stamp
      app.kubernetes.io/instance: stamp
      app.kubernetes.io/component: check
  template:
    metadata:
      labels:
        helm.sh/chart: stamp-0.1.0
        app.kubernetes.io/name: stamp
        app.kubernetes.io/instance: stamp
        app.kubernetes.io/version: "0.1.0"
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/part-of: stamp
        stamp.dev/topology: split
        app.kubernetes.io/component: check
      annotations:
        checksum/config: 9c1ce8280bc8a3cfc0c7991797af90f3ac6ea5967ab6f31e1ced39969b0d4a59
    spec:
      serviceAccountName: stamp
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: stamp
          image: "ghcr.io/d0lim/stamp:0.1.0"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
          # One image, one flag. This is the only argument that differs between
          # the all-in-one topology and the split one.
          args:
            - --roles=check
          ports:
            - name: pep
              containerPort: 8080
              protocol: TCP
          env:
            # R42: the connection string is a credential and arrives by
            # reference. Each tier reads its own Secret, which is how per-role
            # database privileges reach the process (R39) — the grant lives on
            # the login, not in the manifest.
            - name: STAMP_DSN
              valueFrom:
                secretKeyRef:
                  name: "stamp-db-check"
                  key: "dsn"
            # An empty address unbinds a surface; an absent variable would bind
            # it to its default. A tier says what it does not serve.
            - name: STAMP_PEP_ADDR
              value: ":8080"
            - name: STAMP_CONSOLE_ADDR
              value: ""
            - name: STAMP_CALLBACK_ADDR
              value: ""
            - name: STAMP_DB_MIGRATE
              value: "false"
            - name: STAMP_DB_APPLY_GRANTS
              value: "false"
            - name: STAMP_MFA_CIBA_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: "stamp-ciba"
                  key: "client-secret"
            # STAMP_AUDIT_WRITER_ID is deliberately unset: exactly one live
            # process may hold a writer identifier, and the default derives one
            # from the pod's hostname. Setting it to a literal here would make
            # the second replica fail to boot.
          envFrom:
            - configMapRef:
                name: stamp-env
          livenessProbe:
            httpGet:
              path: /healthz
              port: pep
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: pep
            initialDelaySeconds: 2
            periodSeconds: 5
          resources:
            requests:
              cpu: "1"
              memory: 512Mi
          volumeMounts:
            - name: doc-external-targets
              mountPath: "/etc/stamp/documents/external-targets.json"
              subPath: "external-targets.json"
              readOnly: true
            - name: doc-fact-sources
              mountPath: "/etc/stamp/documents/fact-sources.json"
              subPath: "fact-sources.json"
              readOnly: true
            - name: doc-idp-group-sources
              mountPath: "/etc/stamp/documents/idp-group-sources.json"
              subPath: "idp-group-sources.json"
              readOnly: true
            - name: doc-ingest-credentials
              mountPath: "/etc/stamp/documents/ingest-credentials.json"
              subPath: "ingest-credentials.json"
              readOnly: true
            - name: doc-kafka-topics
              mountPath: "/etc/stamp/documents/kafka-topics.json"
              subPath: "kafka-topics.json"
              readOnly: true
            - name: doc-stream-sources
              mountPath: "/etc/stamp/documents/stream-sources.json"
              subPath: "stream-sources.json"
              readOnly: true
      # The documents are mounted, not templated: three of the six may carry a
      # credential and none of them may appear in a manifest.
      volumes:
        - name: doc-external-targets
          secret:
            secretName: "stamp-external-targets"
            items:
              - key: "external-targets.json"
                path: "external-targets.json"
        - name: doc-fact-sources
          secret:
            secretName: "stamp-fact-sources"
            items:
              - key: "fact-sources.json"
                path: "fact-sources.json"
        - name: doc-idp-group-sources
          secret:
            secretName: "stamp-idp-group-sources"
            items:
              - key: "idp-group-sources.json"
                path: "idp-group-sources.json"
        - name: doc-ingest-credentials
          secret:
            secretName: "stamp-ingest-credentials"
            items:
              - key: "ingest-credentials.json"
                path: "ingest-credentials.json"
        - name: doc-kafka-topics
          secret:
            secretName: "stamp-kafka-topics"
            items:
              - key: "kafka-topics.json"
                path: "kafka-topics.json"
        - name: doc-stream-sources
          secret:
            secretName: "stamp-stream-sources"
            items:
              - key: "stream-sources.json"
                path: "stream-sources.json"
---
# Source: stamp/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stamp-decide
  labels:
    helm.sh/chart: stamp-0.1.0
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: stamp
    stamp.dev/topology: split
    app.kubernetes.io/component: decide
    stamp.dev/roles: "decide"
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: stamp
      app.kubernetes.io/instance: stamp
      app.kubernetes.io/component: decide
  template:
    metadata:
      labels:
        helm.sh/chart: stamp-0.1.0
        app.kubernetes.io/name: stamp
        app.kubernetes.io/instance: stamp
        app.kubernetes.io/version: "0.1.0"
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/part-of: stamp
        stamp.dev/topology: split
        app.kubernetes.io/component: decide
      annotations:
        checksum/config: 9c1ce8280bc8a3cfc0c7991797af90f3ac6ea5967ab6f31e1ced39969b0d4a59
    spec:
      serviceAccountName: stamp
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: stamp
          image: "ghcr.io/d0lim/stamp:0.1.0"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
          # One image, one flag. This is the only argument that differs between
          # the all-in-one topology and the split one.
          args:
            - --roles=decide
          ports:
            - name: console
              containerPort: 8081
              protocol: TCP
            - name: callback
              containerPort: 8082
              protocol: TCP
          env:
            # R42: the connection string is a credential and arrives by
            # reference. Each tier reads its own Secret, which is how per-role
            # database privileges reach the process (R39) — the grant lives on
            # the login, not in the manifest.
            - name: STAMP_DSN
              valueFrom:
                secretKeyRef:
                  name: "stamp-db-decide"
                  key: "dsn"
            # An empty address unbinds a surface; an absent variable would bind
            # it to its default. A tier says what it does not serve.
            - name: STAMP_PEP_ADDR
              value: ""
            - name: STAMP_CONSOLE_ADDR
              value: ":8081"
            - name: STAMP_CALLBACK_ADDR
              value: ":8082"
            - name: STAMP_DB_MIGRATE
              value: "false"
            - name: STAMP_DB_APPLY_GRANTS
              value: "false"
            - name: STAMP_MFA_CIBA_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: "stamp-ciba"
                  key: "client-secret"
            # STAMP_AUDIT_WRITER_ID is deliberately unset: exactly one live
            # process may hold a writer identifier, and the default derives one
            # from the pod's hostname. Setting it to a literal here would make
            # the second replica fail to boot.
          envFrom:
            - configMapRef:
                name: stamp-env
          livenessProbe:
            httpGet:
              path: /healthz
              port: console
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: console
            initialDelaySeconds: 2
            periodSeconds: 5
          volumeMounts:
            - name: doc-external-targets
              mountPath: "/etc/stamp/documents/external-targets.json"
              subPath: "external-targets.json"
              readOnly: true
            - name: doc-fact-sources
              mountPath: "/etc/stamp/documents/fact-sources.json"
              subPath: "fact-sources.json"
              readOnly: true
            - name: doc-idp-group-sources
              mountPath: "/etc/stamp/documents/idp-group-sources.json"
              subPath: "idp-group-sources.json"
              readOnly: true
            - name: doc-ingest-credentials
              mountPath: "/etc/stamp/documents/ingest-credentials.json"
              subPath: "ingest-credentials.json"
              readOnly: true
            - name: doc-kafka-topics
              mountPath: "/etc/stamp/documents/kafka-topics.json"
              subPath: "kafka-topics.json"
              readOnly: true
            - name: doc-stream-sources
              mountPath: "/etc/stamp/documents/stream-sources.json"
              subPath: "stream-sources.json"
              readOnly: true
      # The documents are mounted, not templated: three of the six may carry a
      # credential and none of them may appear in a manifest.
      volumes:
        - name: doc-external-targets
          secret:
            secretName: "stamp-external-targets"
            items:
              - key: "external-targets.json"
                path: "external-targets.json"
        - name: doc-fact-sources
          secret:
            secretName: "stamp-fact-sources"
            items:
              - key: "fact-sources.json"
                path: "fact-sources.json"
        - name: doc-idp-group-sources
          secret:
            secretName: "stamp-idp-group-sources"
            items:
              - key: "idp-group-sources.json"
                path: "idp-group-sources.json"
        - name: doc-ingest-credentials
          secret:
            secretName: "stamp-ingest-credentials"
            items:
              - key: "ingest-credentials.json"
                path: "ingest-credentials.json"
        - name: doc-kafka-topics
          secret:
            secretName: "stamp-kafka-topics"
            items:
              - key: "kafka-topics.json"
                path: "kafka-topics.json"
        - name: doc-stream-sources
          secret:
            secretName: "stamp-stream-sources"
            items:
              - key: "stream-sources.json"
                path: "stream-sources.json"
---
# Source: stamp/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stamp-consumer
  labels:
    helm.sh/chart: stamp-0.1.0
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: stamp
    stamp.dev/topology: split
    app.kubernetes.io/component: consumer
    stamp.dev/roles: "consumer"
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: stamp
      app.kubernetes.io/instance: stamp
      app.kubernetes.io/component: consumer
  template:
    metadata:
      labels:
        helm.sh/chart: stamp-0.1.0
        app.kubernetes.io/name: stamp
        app.kubernetes.io/instance: stamp
        app.kubernetes.io/version: "0.1.0"
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/part-of: stamp
        stamp.dev/topology: split
        app.kubernetes.io/component: consumer
      annotations:
        checksum/config: 9c1ce8280bc8a3cfc0c7991797af90f3ac6ea5967ab6f31e1ced39969b0d4a59
    spec:
      serviceAccountName: stamp
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: stamp
          image: "ghcr.io/d0lim/stamp:0.1.0"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
          # One image, one flag. This is the only argument that differs between
          # the all-in-one topology and the split one.
          args:
            - --roles=consumer
          ports:
            - name: callback
              containerPort: 8082
              protocol: TCP
          env:
            # R42: the connection string is a credential and arrives by
            # reference. Each tier reads its own Secret, which is how per-role
            # database privileges reach the process (R39) — the grant lives on
            # the login, not in the manifest.
            - name: STAMP_DSN
              valueFrom:
                secretKeyRef:
                  name: "stamp-db-consumer"
                  key: "dsn"
            # An empty address unbinds a surface; an absent variable would bind
            # it to its default. A tier says what it does not serve.
            - name: STAMP_PEP_ADDR
              value: ""
            - name: STAMP_CONSOLE_ADDR
              value: ""
            - name: STAMP_CALLBACK_ADDR
              value: ":8082"
            - name: STAMP_DB_MIGRATE
              value: "false"
            - name: STAMP_DB_APPLY_GRANTS
              value: "false"
            - name: STAMP_MFA_CIBA_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: "stamp-ciba"
                  key: "client-secret"
            # STAMP_AUDIT_WRITER_ID is deliberately unset: exactly one live
            # process may hold a writer identifier, and the default derives one
            # from the pod's hostname. Setting it to a literal here would make
            # the second replica fail to boot.
          envFrom:
            - configMapRef:
                name: stamp-env
          livenessProbe:
            httpGet:
              path: /healthz
              port: callback
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: callback
            initialDelaySeconds: 2
            periodSeconds: 5
          volumeMounts:
            - name: doc-external-targets
              mountPath: "/etc/stamp/documents/external-targets.json"
              subPath: "external-targets.json"
              readOnly: true
            - name: doc-fact-sources
              mountPath: "/etc/stamp/documents/fact-sources.json"
              subPath: "fact-sources.json"
              readOnly: true
            - name: doc-idp-group-sources
              mountPath: "/etc/stamp/documents/idp-group-sources.json"
              subPath: "idp-group-sources.json"
              readOnly: true
            - name: doc-ingest-credentials
              mountPath: "/etc/stamp/documents/ingest-credentials.json"
              subPath: "ingest-credentials.json"
              readOnly: true
            - name: doc-kafka-topics
              mountPath: "/etc/stamp/documents/kafka-topics.json"
              subPath: "kafka-topics.json"
              readOnly: true
            - name: doc-stream-sources
              mountPath: "/etc/stamp/documents/stream-sources.json"
              subPath: "stream-sources.json"
              readOnly: true
      # The documents are mounted, not templated: three of the six may carry a
      # credential and none of them may appear in a manifest.
      volumes:
        - name: doc-external-targets
          secret:
            secretName: "stamp-external-targets"
            items:
              - key: "external-targets.json"
                path: "external-targets.json"
        - name: doc-fact-sources
          secret:
            secretName: "stamp-fact-sources"
            items:
              - key: "fact-sources.json"
                path: "fact-sources.json"
        - name: doc-idp-group-sources
          secret:
            secretName: "stamp-idp-group-sources"
            items:
              - key: "idp-group-sources.json"
                path: "idp-group-sources.json"
        - name: doc-ingest-credentials
          secret:
            secretName: "stamp-ingest-credentials"
            items:
              - key: "ingest-credentials.json"
                path: "ingest-credentials.json"
        - name: doc-kafka-topics
          secret:
            secretName: "stamp-kafka-topics"
            items:
              - key: "kafka-topics.json"
                path: "kafka-topics.json"
        - name: doc-stream-sources
          secret:
            secretName: "stamp-stream-sources"
            items:
              - key: "stream-sources.json"
                path: "stream-sources.json"
---
# Source: stamp/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stamp-api
  labels:
    helm.sh/chart: stamp-0.1.0
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: stamp
    stamp.dev/topology: split
    app.kubernetes.io/component: api
    stamp.dev/roles: "api"
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: stamp
      app.kubernetes.io/instance: stamp
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        helm.sh/chart: stamp-0.1.0
        app.kubernetes.io/name: stamp
        app.kubernetes.io/instance: stamp
        app.kubernetes.io/version: "0.1.0"
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/part-of: stamp
        stamp.dev/topology: split
        app.kubernetes.io/component: api
      annotations:
        checksum/config: 9c1ce8280bc8a3cfc0c7991797af90f3ac6ea5967ab6f31e1ced39969b0d4a59
    spec:
      serviceAccountName: stamp
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: stamp
          image: "ghcr.io/d0lim/stamp:0.1.0"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
          # One image, one flag. This is the only argument that differs between
          # the all-in-one topology and the split one.
          args:
            - --roles=api
          ports:
            - name: console
              containerPort: 8081
              protocol: TCP
          env:
            # R42: the connection string is a credential and arrives by
            # reference. Each tier reads its own Secret, which is how per-role
            # database privileges reach the process (R39) — the grant lives on
            # the login, not in the manifest.
            - name: STAMP_DSN
              valueFrom:
                secretKeyRef:
                  name: "stamp-db-api"
                  key: "dsn"
            # An empty address unbinds a surface; an absent variable would bind
            # it to its default. A tier says what it does not serve.
            - name: STAMP_PEP_ADDR
              value: ""
            - name: STAMP_CONSOLE_ADDR
              value: ":8081"
            - name: STAMP_CALLBACK_ADDR
              value: ""
            - name: STAMP_DB_MIGRATE
              value: "true"
            - name: STAMP_DB_APPLY_GRANTS
              value: "true"
            - name: STAMP_MFA_CIBA_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: "stamp-ciba"
                  key: "client-secret"
            # STAMP_AUDIT_WRITER_ID is deliberately unset: exactly one live
            # process may hold a writer identifier, and the default derives one
            # from the pod's hostname. Setting it to a literal here would make
            # the second replica fail to boot.
          envFrom:
            - configMapRef:
                name: stamp-env
          livenessProbe:
            httpGet:
              path: /healthz
              port: console
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: console
            initialDelaySeconds: 2
            periodSeconds: 5
          volumeMounts:
            - name: doc-external-targets
              mountPath: "/etc/stamp/documents/external-targets.json"
              subPath: "external-targets.json"
              readOnly: true
            - name: doc-fact-sources
              mountPath: "/etc/stamp/documents/fact-sources.json"
              subPath: "fact-sources.json"
              readOnly: true
            - name: doc-idp-group-sources
              mountPath: "/etc/stamp/documents/idp-group-sources.json"
              subPath: "idp-group-sources.json"
              readOnly: true
            - name: doc-ingest-credentials
              mountPath: "/etc/stamp/documents/ingest-credentials.json"
              subPath: "ingest-credentials.json"
              readOnly: true
            - name: doc-kafka-topics
              mountPath: "/etc/stamp/documents/kafka-topics.json"
              subPath: "kafka-topics.json"
              readOnly: true
            - name: doc-stream-sources
              mountPath: "/etc/stamp/documents/stream-sources.json"
              subPath: "stream-sources.json"
              readOnly: true
      # The documents are mounted, not templated: three of the six may carry a
      # credential and none of them may appear in a manifest.
      volumes:
        - name: doc-external-targets
          secret:
            secretName: "stamp-external-targets"
            items:
              - key: "external-targets.json"
                path: "external-targets.json"
        - name: doc-fact-sources
          secret:
            secretName: "stamp-fact-sources"
            items:
              - key: "fact-sources.json"
                path: "fact-sources.json"
        - name: doc-idp-group-sources
          secret:
            secretName: "stamp-idp-group-sources"
            items:
              - key: "idp-group-sources.json"
                path: "idp-group-sources.json"
        - name: doc-ingest-credentials
          secret:
            secretName: "stamp-ingest-credentials"
            items:
              - key: "ingest-credentials.json"
                path: "ingest-credentials.json"
        - name: doc-kafka-topics
          secret:
            secretName: "stamp-kafka-topics"
            items:
              - key: "kafka-topics.json"
                path: "kafka-topics.json"
        - name: doc-stream-sources
          secret:
            secretName: "stamp-stream-sources"
            items:
              - key: "stream-sources.json"
                path: "stream-sources.json"
---
# Source: stamp/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stamp-console
  labels:
    helm.sh/chart: stamp-0.1.0
    app.kubernetes.io/name: stamp
    app.kubernetes.io/instance: stamp
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: stamp
    stamp.dev/topology: split
    app.kubernetes.io/component: console
    stamp.dev/roles: "console"
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: stamp
      app.kubernetes.io/instance: stamp
      app.kubernetes.io/component: console
  template:
    metadata:
      labels:
        helm.sh/chart: stamp-0.1.0
        app.kubernetes.io/name: stamp
        app.kubernetes.io/instance: stamp
        app.kubernetes.io/version: "0.1.0"
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/part-of: stamp
        stamp.dev/topology: split
        app.kubernetes.io/component: console
      annotations:
        checksum/config: 9c1ce8280bc8a3cfc0c7991797af90f3ac6ea5967ab6f31e1ced39969b0d4a59
    spec:
      serviceAccountName: stamp
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: stamp
          image: "ghcr.io/d0lim/stamp:0.1.0"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
          # One image, one flag. This is the only argument that differs between
          # the all-in-one topology and the split one.
          args:
            - --roles=console
          ports:
            - name: console
              containerPort: 8081
              protocol: TCP
          env:
            # R42: the connection string is a credential and arrives by
            # reference. Each tier reads its own Secret, which is how per-role
            # database privileges reach the process (R39) — the grant lives on
            # the login, not in the manifest.
            - name: STAMP_DSN
              valueFrom:
                secretKeyRef:
                  name: "stamp-db-console"
                  key: "dsn"
            # An empty address unbinds a surface; an absent variable would bind
            # it to its default. A tier says what it does not serve.
            - name: STAMP_PEP_ADDR
              value: ""
            - name: STAMP_CONSOLE_ADDR
              value: ":8081"
            - name: STAMP_CALLBACK_ADDR
              value: ""
            - name: STAMP_DB_MIGRATE
              value: "false"
            - name: STAMP_DB_APPLY_GRANTS
              value: "false"
            - name: STAMP_MFA_CIBA_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: "stamp-ciba"
                  key: "client-secret"
            # STAMP_AUDIT_WRITER_ID is deliberately unset: exactly one live
            # process may hold a writer identifier, and the default derives one
            # from the pod's hostname. Setting it to a literal here would make
            # the second replica fail to boot.
          envFrom:
            - configMapRef:
                name: stamp-env
          livenessProbe:
            httpGet:
              path: /healthz
              port: console
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: console
            initialDelaySeconds: 2
            periodSeconds: 5
          volumeMounts:
            - name: doc-external-targets
              mountPath: "/etc/stamp/documents/external-targets.json"
              subPath: "external-targets.json"
              readOnly: true
            - name: doc-fact-sources
              mountPath: "/etc/stamp/documents/fact-sources.json"
              subPath: "fact-sources.json"
              readOnly: true
            - name: doc-idp-group-sources
              mountPath: "/etc/stamp/documents/idp-group-sources.json"
              subPath: "idp-group-sources.json"
              readOnly: true
            - name: doc-ingest-credentials
              mountPath: "/etc/stamp/documents/ingest-credentials.json"
              subPath: "ingest-credentials.json"
              readOnly: true
            - name: doc-kafka-topics
              mountPath: "/etc/stamp/documents/kafka-topics.json"
              subPath: "kafka-topics.json"
              readOnly: true
            - name: doc-stream-sources
              mountPath: "/etc/stamp/documents/stream-sources.json"
              subPath: "stream-sources.json"
              readOnly: true
      # The documents are mounted, not templated: three of the six may carry a
      # credential and none of them may appear in a manifest.
      volumes:
        - name: doc-external-targets
          secret:
            secretName: "stamp-external-targets"
            items:
              - key: "external-targets.json"
                path: "external-targets.json"
        - name: doc-fact-sources
          secret:
            secretName: "stamp-fact-sources"
            items:
              - key: "fact-sources.json"
                path: "fact-sources.json"
        - name: doc-idp-group-sources
          secret:
            secretName: "stamp-idp-group-sources"
            items:
              - key: "idp-group-sources.json"
                path: "idp-group-sources.json"
        - name: doc-ingest-credentials
          secret:
            secretName: "stamp-ingest-credentials"
            items:
              - key: "ingest-credentials.json"
                path: "ingest-credentials.json"
        - name: doc-kafka-topics
          secret:
            secretName: "stamp-kafka-topics"
            items:
              - key: "kafka-topics.json"
                path: "kafka-topics.json"
        - name: doc-stream-sources
          secret:
            secretName: "stamp-stream-sources"
            items:
              - key: "stream-sources.json"
                path: "stream-sources.json"
