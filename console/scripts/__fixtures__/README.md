# 위반 픽스처

여기 있는 파일들은 컴파일되지 않고 실행되지도 않는다. `scripts/check-contract.mjs`가
**여전히 실패하는지**를 확인하기 위한 입력이다.

통과하는 검사만 있으면 검사가 살아 있다는 증거가 없다. 각 디렉터리는 D19가 막으려는
한 가지 방식을 재현하고, `check-contract.test.mjs`가 그 디렉터리를 검사기에 넣어
기대한 규칙이 실제로 걸리는지 본다.

`tsconfig.json`의 `include`는 `src`만 담으므로 이 파일들은 타입 검사에 들어가지 않는다.
