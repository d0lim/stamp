# 핸드오프 — 2026-08-14

`main` = `b52928c`. 열린 이슈 0. `make land` 전량 그린. **신뢰도 라운드 5회 전부 완료.**

이 문서는 **다음 사람이 문서만 읽고 이어받을 수 있게** 하려고 있다. 무엇이 참이고, 무엇이 아직 참이 아니며, 왜 그렇게 뒀는지.

---

## 1. 지금 당장 해야 하는 것 하나

```
gh workflow run release.yml --ref main -f version=0.1.0 -f unreleased=true
```

**첫 태그를 밀기 전에 이걸 한 번 돌려라.** 위험은 0이다 — `publish`는 `github.event_name == "push"`일 때만 참이므로 **dispatch는 아무것도 발행할 수 없다.**

**왜 중요한가.** 라운드 4가 실행 없이 증명해서 찾은 것: 지금 상태로 태그를 밀면 `image` 잡이 이미지를 push하고 서명하고 `latest`를 옮긴 **뒤**, `artifacts` 잡이 첫 스텝에서 죽는다. `scripts/release-artifacts.sh`가 `CHANGELOG.md`에서 `## [<version>]`를 읽는데 이 리포에는 `## [Unreleased]`뿐이기 때문이다. `image`는 `artifacts`에 의존하지 않으므로(`needs: gates`), **레지스트리에 서명된 `X.Y.Z`와 옮겨진 `latest`가 남고 차트·SBOM·릴리즈가 없는 상태가 되돌릴 수 없이 만들어진다.**

그 구멍 자체는 아직 **고치지 않았다** — 라운드 4의 지시가 "증명하되 릴리즈 절차를 바꾸지 말 것"이었다. **첫 태그 전에 CHANGELOG에 실제 버전 섹션을 넣어야 한다.**

리허설이 닫지 못하는 것(첫 실제 릴리즈에서만 알 수 있음): `ghcr.io` write 권한, multi-arch 빌드, cosign keyless OIDC 왕복, `helm push`, `gh release create`, 원격 이미지 `syft scan`, 그리고 나온 서명이 제3자에게 검증되는지.

---

## 2. 이 세션이 한 일 — 여덟 PR

| PR | 내용 |
|---|---|
| #51 | 열린 이슈 여섯 + 그 과정이 만든 P1 다섯 |
| #52 | 마지막 무예산 표면, 유일한 가드, 부팅 경합 둘 |
| #53 | error 코드 어휘를 기계가 대조 (신뢰도 R1) |
| #54 | 의존성 실패 시 fail-closed 증명 — **fail-open 둘 발견** (R2) |
| #55 | 시스템이 하는 말과 하는 일을 일치 (R3) |
| #56 | 첫 릴리즈가 절반만 발행하고 실패했을 것 — 실행 없이 증명 (R4) |
| #58 | 부팅 경합을 없애고, **가드가 가드가 아니었던 것**을 고침 (R5 전반) |
| #59 | 포화 경계 셋에 관측된 값을 붙이고, flaky한 동시성 가드를 고침 (R5 후반) |

신뢰도 라운드 **5회 전부 완료.**

---

## 3. 이 리포에서 반복해서 나온 결함 부류

다음 사람이 알아야 할 가장 중요한 것. 같은 모양이 **일곱 번** 나왔다.

### 3.1 "구현됐지만 도달할 수 없다" (7회)

decide()에 HTTP 엔드포인트가 없었고, 체크포인트 서브시스템에 호출자가 없었고, 위임 MFA를 걸을 수 없었고, split 토폴로지가 `POST /decisions`를 서빙할 수 없었고, helm이 돌아올 곳 없는 콜백을 받아들였고, `CIBA.Poll`에 호출자가 없었고, **릴리즈 경로의 넷이 모든 입력에서 도달 불가능했다.**

**패턴**: 도달할 수 없는 경로는 자기 결함도 숨긴다. PKCE 누락이 데모가 브라우저 왕복을 안 돌아서 안 드러난 것이 예다.

### 3.2 "가드가 초록인데 실물이 빨갛다" (7회 이상)

**여섯 번째와 일곱 번째는 마지막 라운드가 스스로 만났다.** 하나는 제가 만들었고 하나는 이미 있었다. 둘은 **같은 실수의 거울상**이다.

- **너무 넓은 창** (#58): 부팅 경합을 고치고 붙인 "운에 기대지 않는 가드"가 `Migrate` 전체에서 동시성 피크를 쟀다. 마이그레이션 락이 최대 90초 폴링하므로 진 부트는 **구조적으로** 거기 머문다 — 피크는 언제나 4였고, **미수정 코드로 초록이었던 143회에서도 그랬다.** 그 단언은 한 번도 발동하지 않았을 것이다. 그런데 뮤테이션 문서에 "이 라운드가 닫은 자리"라고 적었다.
- **너무 좁은 창** (#59): `internal/stream`의 배리어는 뮤텍스 안(나노초)에서 겹침을 쟀다. 부하 걸린 CI에서 512개 중 동시에 안에 있던 것이 **1개**였고 빌드가 빨개졌다 — 주제는 멀쩡한데 가드가 자기 이유로 실패했다. 게다가 `Allow`는 뮤텍스를 잡으므로 임계 구역엔 **정의상 하나만** 있다. 단언 자체에 혼동이 있었다.

**규칙**: "동시에 X 안에 있었나"를 재려면 **X의 길이가 증명하려는 것과 관계있어야 한다.** 그리고 겹침은 **재는 것보다 구성하는 것**이 낫다 — 랑데부로 모두를 같은 지점에 세운 뒤 함께 놓으면 스케줄러 운이 사라진다.

두 번째 교훈: **뮤테이션이 red가 된 것만으로는 부족하다.** 130회 중 1회 red를 적어 놓고 "확인함"이라 결론냈다. 그 숫자는 **CI 한 번이 이것을 잡을 확률 약 1%**라는 뜻이다. 회수를 기록하는 것과 회수가 뜻하는 바를 읽는 것은 다르다.


`quickstart.sh`의 silent-pass 가드 넷. 콘솔 테스트가 스텁으로 서버가 안 보내는 코드를 시험. `chart_test.go`의 손으로 쓴 map이 차트와 **함께** 틀림. CI가 exit 7로 조용히 죽음. 릴리즈가 초록으로 아무것도 발행 안 함.

**그래서 이 리포의 규칙**: **모든 새 검사는 자기 검사를 갖는다.** 심어둔 드리프트에서 실제로 실패하는 것을 확인하고, **픽스처가 아니라 실물**(실제 차트 스냅샷, 실제 계약 문서, 실제 콘솔 소스)에 심어서 확인한다.

### 3.3 열거를 그만두는 것이 답이 아니다 — 사건이 아니라 기제를 열거하라

부팅 경합이 **세 번** 났고 세 번 다 만난 SQLSTATE만 처리했다. `CREATE TABLE`은 사전 확인을 두 번 하고(`pg_class` → 42P07, `pg_type` → 42710) 둘 다 통과하면 유니크 인덱스가 잡는다(23505). **어느 쪽이 지는지는 스케줄링 운이고, 셋은 닫힌 집합이다.** 기제 표를 만들면 열거가 끝난다. 사건 목록은 다음 사건에서 또 모자란다.

그리고 **클래스 단위로 넓히는 것은 답이 아니었다** — 클래스 42에 `42501 insufficient_privilege`가 있어서, 최소 권한 배포에서 권한 거부를 "이미 있다"로 읽고 부팅을 성공시킨다.

### 3.4 손으로 쓴 목록은 자기가 지키는 것과 함께 틀린다

`chart_test.go`의 역할→표면 map이 그랬다. 그래서 `mounted-routes.json`, `error-codes.json`이 **코드에서 유도**된다.

**측정된 예**: 제가 손으로 센 error 코드가 28개였는데 실제로는 **48개**였다. 네 error 테이블이 내는 18개가 호출 지점에 리터럴로 전혀 안 나타난다.

---

## 4. 포화 경계 — 이제 관측된 값이 있다

셋 다 측정됐다. 근거는 `docs/operations/failure-modes.md`의 "Saturation" 절이고, **각 행이 자기를 지키는 테스트를 인용한다.**

| 경계 | 값 | 관측된 것 |
|---|---|---|
| 제한기 표 | 8192 | 가득 차고 sweep이 회수 못하면 **거부**. 이미 있는 키는 계속 부과. 리필 뒤 회복 |
| 감사 버퍼 | 4096 **/ flush 간격** | 정확히 capacity+1에서 드롭 시작, gap 마커가 개수와 창을 남김, 첫 유실이 경보 |
| 경보 래치 | threshold 1 | 유실이 기록되고 **동시에** 큐가 절반 아래로 빠져야 풀림 |
| 인스턴스별 예산 | — | **2 인스턴스 × 버스트 3 = 6 admitted.** 문서의 "N배"가 관측된 숫자를 가짐 |

**관측을 먼저 한 것이 값을 세 번 했다** (KTD4). 가정이 세 번 틀렸다:

- `STAMP_AUDIT_FAIL_CLOSED`가 체인 실패뿐 아니라 **포화에서도 걸린다.** 어디에도 적혀 있지 않던 질문이었다.
- **check 표면에는 예산이 없다.** R43이 상태를 만드는 연산만 이름하므로 공백이 아니라 범위다. 부피로 막아야 하면 게이트웨이의 일이다.
- **decide 거부는 429가 아니라 `200` + `reason=rate_limited`다.** 4xx로 알림을 걸면 이 표면의 속도 제한을 못 본다.

## 5. 알려진 채로 남긴 것 — 근거와 함께

전부 `docs/residual-review-findings/`와 각 계획서에 근거가 있다.

| 항목 | 왜 남겼나 |
|---|---|
| **check 표면이 감사 flush 간격(1초) 동안 감사 없는 allow를 낸다** — 1초 간격에서 **769–826 ms 동안 276–364개** 측정 | R32가 판정 경로 비용 때문에 비동기 버퍼를 의도적으로 골랐다. 판정마다 동기 append는 그 거래를 뒤집는다. **약속을 정확하게** 고치고 창을 테스트로 고정했다 |
| **인스턴스별 예산 — N 레플리카가 N배를 허용한다** (측정됨) | 분산 카운터는 자체 실패 양식을 갖는다. 실제 함대 상한인 **미결 결정 캡은 데이터베이스에서 세므로 함대 전체에 걸린다** |
| **부팅 경로의 advisory lock 둘이 아직 무한 대기** — `ApplyGrants`, `Checkpoint` | #58이 새로 더한 자리에만 `lock_timeout`을 걸었다. 셋을 함께 고치는 것이 자연스러운 후속이고 같은 한 줄이면 된다 |
| **차트 liveness 예산(약 35초)이 `migrationLockWait`(90초)보다 짧다** | `Lock()`이 설계된 90초 대기가 차트 기본값에서 도달할 수 없다. 어느 예산이 옳은지는 배포 정책 판단이다 |
| step-up 천장이 여전히 공유라 소진 가능 | **해악은 제거됐다** — 셰딩이 기록을 남기지 않는다. 남은 건 지연이고, 없애려면 분산 예산(아키텍처 결정) |
| 타이밍 부채널 (바이트는 같고 시간은 다름) | lifecycle의 모양을 바꿔야 한다. 자기 라운드가 필요 |
| 인제스트 rate 정규화가 나머지 넷과 갈라짐 | 고치면 그것에 맞춰 설정한 배포의 의미가 밑에서 바뀐다 |
| 손상된 challenge detail이 404 아닌 500 | "읽을 수 없는 detail = 대상 아님"으로 정하는 fail-closed 판단 필요 |
| `schema-read` 등 호출되지 않는 엔드포인트 5개 | `schema-read`는 **결정이 아니라 공백** — 빌더가 항상 빈 스키마에서 시작해 작성자가 무엇을 대체하는지 못 본다 |
| `delay-cancel`에 콘솔 화면 없음 | 계약에 있는 표면. 화면 만들기는 새 기능이라 최소 목표 밖 |
| 필드명(JSON 태그) 대조 | error 코드 어휘는 기계가 강제하지만 필드명은 손 검증뿐 |

---

## 6. 운영 메모

- **`golangci-lint`가 삭제된 sibling 워크트리 파일로 빨개질 수 있다.** `golangci-lint cache clean` 후 재실행. 실제로 이 세션에서 두 번 시간을 먹었다.
- **워크트리 base가 일관되지 않다.** `Agent`의 worktree isolation이 때로 `main`에서, 때로 현재 HEAD에서 분기한다. **워커 프롬프트에 `git reset --hard <branch>`를 명시**하는 것이 유일하게 믿을 만하다.
- **teammate로 띄운 에이전트가 결과를 자주 잃는다.** 코드 리뷰 여덟 렌즈 중 일곱이 그랬다. `run_in_background: true`인 `Agent`는 한 번도 잃지 않았다.
- **동시성 테스트는 로컬에서 초록이어도 CI에서 다르게 논다.** #59가 고친 flake는 로컬 `-count=5`에서 한 번도 안 났고 CI에서 바로 났다. 부하 걸린 러너에서 Go 스케줄러가 512개를 직렬로 돌린다. 동시성 단언을 쓸 때 `GOMAXPROCS=1`과 `GOMAXPROCS=2`로도 돌려 봐라 — 랑데부로 구성했다면 셋 다 결정적이다.

---

## 7. 읽어야 할 문서

| 문서 | 무엇이 있나 |
|---|---|
| `docs/operations/failure-modes.md` | 의존성 실패 시 각 표면이 실제로 답하는 것. **각 행이 그것을 지키는 테스트를 인용한다** |
| `docs/operations/release.md` | 릴리즈 조합 표와 첫 dispatch에서 눈으로 볼 것 |
| `docs/testing/mutation-matrix.md` | 뮤테이션 감사. 다음 라운드가 이어 쓰는 형식 |
| `docs/residual-review-findings/` | 라운드별 잔여 findings |
| `docs/decisions/stamp-decision-log.md` | D-ID 정본 |
| `docs/plans/2026-08-07-001-...` | R-ID 정본 |

---

## 8. 이어받는 사람에게

이 리포의 검사들은 **자기가 검사임을 증명하도록** 만들어져 있다. 새 검사를 더한다면 같은 기준을 지켜라 — 심어둔 드리프트에서 실제로 실패하는 것을, **픽스처가 아니라 실물에** 심어서 확인하라.

그리고 마지막 라운드가 더한 것: **가드를 만들었으면 그 가드가 실제로 실패하는지 확인하고, 몇 번 중 몇 번인지 세어라.** "red를 봤다"는 충분하지 않다. 130회 중 1회 red는 red가 아니라 거의 초록이다. 결정적으로 만들 수 있으면 결정적으로 만들어라 — 이 라운드의 세 가드는 전부 1회 중 1회다.

그리고 이 세션에서 반복해서 확인된 것 하나: **재현이 안 되면 재현을 더 시도하는 대신 시스템이 말하게 만들어라.** CI의 `docker build` flakiness를 로컬에서 세 번 재현 실패했고, 진단 출력을 고치자 첫 실패에서 원인(부팅 경합)이 한 줄로 나왔다.
