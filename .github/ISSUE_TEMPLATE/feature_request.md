---
name: Feature request
about: Propose a capability or a change in behaviour
labels: enhancement
---

## The problem

<!-- What you cannot do today, and what you are doing instead. Concrete beats
general — "I need an approval to expire on a schedule" beats "more flexible
challenges". -->

## Why the engine, and not the caller

<!-- STAMP's boundary is deliberate: it returns obligations and the caller
enforces them. If this belongs inside the engine, say what breaks when it lives
outside. -->

## Does it touch a public contract?

<!-- The policy schema, the challenge interface, and the decision API are
semver-versioned (R11). A change to any of them is a bigger decision than one
behind them. -->

## Alternatives you considered
